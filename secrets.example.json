{
  "_comment": "Copy this file to secrets.json (which is gitignored) and paste in fresh tokens. Do NOT commit secrets.json. Capture tokens from a getCustomerDigitalReceipts request in browser DevTools -> Network: the Authorization-header bearer is firebase_token, the body's token is auth0_token, and deviceId is in the body too. The firebase_token lasts ~60 min.",
  "firebase_token": "PASTE_FIREBASE_BEARER_FROM_AUTHORIZATION_HEADER",
  "auth0_token": "PASTE_AUTH0_TOKEN_FROM_REQUEST_BODY",
  "device_id": "PASTE_DEVICE_ID_FROM_REQUEST_BODY"
}
